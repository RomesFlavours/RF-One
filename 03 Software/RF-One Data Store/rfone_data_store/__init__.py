"""RF-One Data Store — the vendor-independent canonical Restaurant operational database.

This package defines the physical schema only (TASK_DATABASE_001). It does not
ingest data from any source system and does not compute KPIs.
"""
