"""ResumeSource -> RawResume -> ResumeParser -> CandidateCVProfile pipeline
implementation (task §6, §18). `resolve_parser()` is the single entry point
the web app should use — it hides the real-AI/demo-fallback decision.
"""
