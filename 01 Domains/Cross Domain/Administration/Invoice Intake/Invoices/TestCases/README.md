# TestCases

Committed fixtures for validating Invoice Intake acquisition/parsing behavior.

**Only synthetic or deliberately sanitized documents may be committed here** — no real Rome's Flavours (or any other real operational) invoice, receipt, or scan. Real documents belong in `Invoices/Raw/`, which is git-ignored (see root `.gitignore`) and never committed.
